# Accelerators

This contract owns the measurement chain. It does not own CUDA, SP1,
or a Rust ingest gate.

Those accelerators, if a consumer asks for them, are CSE
`rust-effort-gate`, default refuse. A bound RCI digest is not Beam-B1
evidence and is not a point-to-IfcGuid bind.
