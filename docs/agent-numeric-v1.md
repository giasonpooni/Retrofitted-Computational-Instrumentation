# Agent numeric methods

No agent-written numeric method enters this repo without a test against a
closed-form or published reference value. Not a golden file of its own
prior output.

`uncertainty-budget-v1` is the first such method. Traceability stays
`none_claimed` until an accredited certificate changes that string.
